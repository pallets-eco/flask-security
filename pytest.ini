[pytest]
addopts = -rs --cache-clear --strict-markers
markers =
    settings
    babel
    changeable
    confirmable
    registerable
    two_factor
    recoverable
    passwordless
    trackable
    unified_signin
